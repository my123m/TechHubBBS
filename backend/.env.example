# ==================== 必填：JWT 签名密钥 ====================
# 强制要求：缺失 JWT_SECRET 时后端拒绝启动（fail-fast），杜绝使用公开默认密钥。
# 取值要求：
#   - 内容为 Base64 编码的字节串，解码后长度 >= 32 字节（256 bits，HMAC-SHA256 安全下限）。
#   - 生产环境必须使用强随机密钥，可通过以下命令生成：
#       openssl rand -base64 48
#   - 严禁复用本文件中的示例值用于任何非本地调试环境。
# 以下示例值为 "test-secret-key-for-jwt-token-generation-must-be-at-least-256-bits-long-enough"
# 的 Base64 编码，仅供本地调试；任何对外部署必须替换。
JWT_SECRET=dGVzdC1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tZ2VuZXJhdGlvbi1tdXN0LWJlLWF0LWxlYXN0LTI1Ni1iaXRzLWxvbmctZW5vdWdo

# ==================== 可选：CORS 跨域白名单 ====================
# 本地开发无需配置（application.yml 已内置 localhost:5173 默认值）。
# 生产环境必须通过 CORS_ORIGINS 设置前端域名，缺失时后端拒绝启动（fail-fast）。
# 格式：逗号分隔多个域名，例：
#   CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com
# CORS_ORIGINS=

# AI 服务配置（可选 — 不配置则 AI 摘要/问答功能不可用）
AI_PROVIDER=openai
AI_API_KEY=your-api-key-here
AI_API_URL=https://api.openai.com/v1
AI_MODEL=gpt-3.5-turbo